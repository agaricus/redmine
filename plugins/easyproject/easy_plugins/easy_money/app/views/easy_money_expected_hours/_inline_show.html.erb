<%= content_tag :span, l(:label_easy_money_project_index_sum_hours) -%>
<%= content_tag :span, l(:label_easy_money_hour, :hours => expected_hours) -%>
<% if User.current.allowed_to?(:easy_money_manage_expected_payroll_expense, project) && entity_type == 'Project' -%>
  <%= link_to('', {:controller => 'easy_money_expected_hours', :action => 'inline_edit', :project_id => project.id, :entity_type => entity_type, :entity_id => entity_id}, :class => 'icon icon-edit', :title => l(:button_edit), :remote => true) %>
<% end -%>