<%= labelled_form_for @group, :url => group_path(@group), :html => {:method => :put} do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= submit_tag l(:button_save), :class => 'button-1' %>
<% end %>